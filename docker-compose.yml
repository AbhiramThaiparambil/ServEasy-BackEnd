version: '3.8'

services:
  # MongoDB Database
  database:
    image: mongo:latest
    ports:
      - "27017:27017"

  # Backend Node.js App
  backend:
    build: .
    ports:
      - "5000:5000"
    environment:
      - MONGO_URI=mongodb://database:27017/serveasy
    depends_on:
      - database